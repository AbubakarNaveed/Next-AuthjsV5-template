import React from "react";
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Detail Page",
  description: "This is detail page of the website",
};
const Details = () => {
  return <div>Details</div>;
};

export default Details;
